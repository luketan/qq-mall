<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//configs.mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.honglinktech.zbgj.dao.NewProductImagesDao">
    <!-- Mappers -->

    <select id="findById"
            parameterType="int"
            resultType="com.honglinktech.zbgj.entity.NewProductImages">
        select
        id,
        product_id as productId,
        images,
        sort,
        update_date as updateDate,
        delete_flag as deleteFlag
        from tb_new_product_images where id = #{id}

    </select>


    <select id="findByProductId"
            parameterType="int"
            resultType="java.util.Map">
        select
        id as imagesId,
        images as imagesAddress,
        sort
        from tb_new_product_images where product_id = #{productId} AND delete_flag=0 order by sort is null,sort ASC
        ,update_date asc,id ASC

    </select>


    <!-- 保存 -->
    <insert id="save"
            parameterType="com.honglinktech.zbgj.entity.NewProductImages"
            useGeneratedKeys="true" keyProperty="id">
        <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID() AS ID
        </selectKey>
        INSERT INTO tb_new_product_images
        (
        product_id,
        images,
        sort,
        update_date,
        delete_flag
        )
        VALUES
        (
        #{productId},
        #{images},
        #{sort},
        now(),
        #{deleteFlag}
        )
    </insert>

    <!-- 更新-->
    <update id="update"
            parameterType="com.honglinktech.zbgj.entity.NewProductImages">
        UPDATE tb_new_product_images
        SET
        product_id=#{productId},
        images=#{images},
        <if test="sort!=null">
            sort=#{sort},
        </if>
        <if test="deleteFlag!=null">
            delete_flag= #{deleteFlag},
        </if>
        update_date=now()
        WHERE
        id = #{id}
    </update>


    <!-- 删除-->
    <update id="delete"
            parameterType="int">
        UPDATE tb_new_product_images
        SET
        delete_flag=1,
        update_date=now()
        WHERE
        id = #{id}
    </update>
    <delete id="deleteByProductId" parameterType="int">
        DELETE FROM tb_new_product_images WHERE product_id = #{productId}
    </delete>

</mapper>
